# This project keeps its configurable values (Formspree, MailerLite, social
# links) in src/data/site.ts rather than env vars, so there's nothing required
# here. This file exists only as a placeholder for any secrets you add later.
